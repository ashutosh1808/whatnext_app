# whatnext_app


## MySQL Part

To deploy this project run

```bash
  mysql -u[uname] -p[passwd]

  mysql> use kc_2feb23;
  mysql> create table student(name varchar(20), 
        -> choice varchar(10)
        -> );
```

